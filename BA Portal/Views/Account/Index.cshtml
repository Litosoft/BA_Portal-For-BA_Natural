@model IEnumerable<BA_Portal.Models.ApplicationUser>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>User Management</h2><br />

    There are at this time 5 levels of user authorization: <br />
1. CanManageUsers (SuperAdmin) <br />
2. CanEdit (Admin) <br />
3. CanView (may view record but not edit)<br />
4. User (no role. May login but may not view anything)<br />
5. Anonymous (may not login) <br />

    Currently SuperAdmin (the CanUserManage role) can create other admins of the CanEdit level (admin) by clicking on this link: @Html.ActionLink("New CanEdit Admin", "Register", "Account") <br />
    <br />


    <br /><br />
    <h3>List of Users:</h3> 


    @{
        int i = 0;

        <table class="table">
            <tr>
                <th>
                    <strong>#</strong>
                </th>
                <th>
                    <strong>Username</strong>
                </th>

                <th>

                </th>
                <th>
                    <strong>Options</strong>
                </th>
            </tr>



            @foreach (var user in Model)
            {


                i++;
                <tr>
                    <td>
                        <strong>@i. </strong>

                    </td>
                    <td>
                        @user.UserName


                    </td>
                    <td></td>

                    <td>

                        @Html.ActionLink("Delete", "DeleteUser", new { id = user.Id }) | | |
                        @Html.ActionLink("Details", "UserDetails", new { id = user.Id })
                    </td>

                </tr>
            }
        </table>



    }

<br />
<span id="border01">User Count: @ViewBag.count</span>



